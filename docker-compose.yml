services:
  discordbot:
    build: .
    container_name: discordbot
    environment:
      # Read from the host environment or an .env file next to this compose file.
      - DISCORD_TOKEN=${DISCORD_TOKEN}
    restart: unless-stopped
